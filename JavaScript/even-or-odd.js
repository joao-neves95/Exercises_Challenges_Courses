/*
Finding if a number is even or odd.
JavaScript.
*/

function even_or_odd(number) {
  // When a even number is divided by 2, the remainder is 0.
  if (number % 2 == 0)
    return "Even"
  else
    return "Odd"
}
