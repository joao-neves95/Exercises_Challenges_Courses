apiVersion: v1
kind: Service
metadata:
  name: eshop-catalog-api
spec:
  # `NodePort` for localhost.
  type: NodePort
  selector:
    app: eshop-catalog-api
  ports:
    - protocol: TCP
      port: 30000
      targetPort: 80
      # `nodePort` here is to run locally.
      # nodePort: 30000
